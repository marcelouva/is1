<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vincular Tarea a Equipo</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Estilos generales para el cuerpo */
        body {
            font-family: 'Inter', sans-serif; /* Fuente moderna y limpia */
            background-color: #f8f9fa; /* Un gris muy claro, casi blanco, para el fondo */
        }
        /* Personalización de los anillos de enfoque para una mejor visibilidad */
        input:focus, select:focus, button:focus {
            outline: none; /* Elimina el contorno predeterminado del navegador */
            box-shadow: 0 0 0 3px rgba(66, 153, 225, 0.5); /* Anillo de enfoque azul sutil */
        }
    </style>
</head>
<body class="bg-gray-50 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-md border border-gray-200">
        <h1 class="text-4xl font-extrabold text-center text-gray-800 mb-8 tracking-tight">Vincular Tarea a Equipo</h1>

        {{! Mensaje de éxito o error (opcional, si lo envías desde el servidor) }}
        {{#message}}
            <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg relative mb-4 shadow-sm" role="alert">
                <p class="font-semibold">¡Éxito!</p>
                <span class="block sm:inline text-sm">{{message}}</span>
            </div>
        {{/message}}
        {{#error}}
            <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg relative mb-4 shadow-sm" role="alert">
                <p class="font-semibold">¡Error!</p>
                <span class="block sm:inline text-sm">{{error}}</span>
            </div>
        {{/error}}

        <form action="/teams/tasks/link" method="post" class="space-y-6">
            <div>
                <label for="team_id" class="block text-gray-700 text-sm font-medium mb-2">Seleccionar Equipo:</label>
                <select id="team_id" name="team_id" required
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:border-blue-500 focus:ring-blue-500 text-gray-800 shadow-sm transition duration-150 ease-in-out">
                    <option value="" class="text-gray-500">-- Selecciona un Equipo --</option>
                    {{#teams}}
                        <option value="{{id}}">{{name}}</option>
                    {{/teams}}
                </select>
            </div>

            <div>
                <label for="task_id" class="block text-gray-700 text-sm font-medium mb-2">Seleccionar Tarea:</label>
                <select id="task_id" name="task_id" required
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:border-blue-500 focus:ring-blue-500 text-gray-800 shadow-sm transition duration-150 ease-in-out">
                    <option value="" class="text-gray-500">-- Selecciona una Tarea --</option>
                    {{#tasks}}
                        <option value="{{id}}">{{name}}</option>
                    {{/tasks}}
                </select>
            </div>

            <button type="submit"
                    class="w-full bg-blue-600 text-white font-semibold py-3 px-4 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-4 focus:ring-blue-300 focus:ring-offset-2 transition duration-300 ease-in-out transform hover:scale-105 shadow-md">
                Vincular Tarea
            </button>

            <div class="text-center mt-6">
                <a href="/" class="text-blue-600 hover:text-blue-800 text-sm font-medium transition duration-150 ease-in-out">Volver al inicio</a>
            </div>
        </form>
    </div>
</body>
</html>
