<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Lista de Usuarios</title>
    <style>
        body {
            font-family: Roboto, Arial, sans-serif;
            background-color: #f9f9f9;
            color: #202124;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #1a73e8;
            font-size: 24px;
            margin-bottom: 20px;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            background-color: #fff;
            margin-bottom: 10px;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 1px 3px rgba(60, 64, 67, 0.3);
        }

        li:last-child {
            margin-bottom: 0;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            color: #1a73e8;
            font-weight: 500;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Usuarios Registrados</h1>
    <ul>
        {{#users}}
        <li><strong>ID:</strong> {{id}}, <strong>Nombre:</strong> {{name}}</li>
        {{/users}}
        {{^users}}
        <li>No hay usuarios registrados.</li>
        {{/users}}
    </ul>
    <p><a href="/">Volver al inicio</a></p>
</body>
</html>
